version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - frontend_node_modules:/app/node_modules
    ports:
      - "5174:5173"

volumes:
  frontend_node_modules:
